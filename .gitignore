# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
# wrangler's local state for the editing worker
.wrangler/
# The worker's secrets for `wrangler dev` — the same four values that are
# `wrangler secret put` in production. Never committed: this repo is public.
.dev.vars
*.local
# Local editing config; the committed .env.example documents the two keys.
.env
dev-dist/
coverage/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# local docs 
/docs/
__pycache__/
*.pyc
